import { css } from '@emotion/core';
const Theme = {
  black: '#383838',
  border: `2px solid #383838`
};

export const resetStyles = css`
  background-image: url('/public/debut-light.png');
`;

export default Theme;
